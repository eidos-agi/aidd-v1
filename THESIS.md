# Thesis — aidd-v1

Products get `product-v1`, then `product-v2`, then `product-v3`. Each is a generation repo. The public `product` is a copy of whoever last won.

This generation is the first bash. `aidd` currently shows a copy of it. Further work happens here. `aidd-v2` is the next evolution, opened on a trigger, copied back only if it beats this.

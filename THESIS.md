# Thesis — aidd-v1

Products get `product`, then `product-v1`, then `product-v2`. Each generation is a repo. AI bashes in the generation. Winners get pushed back to the public name.

AIDD dogfoods that layout on itself.

v1 does not inherit champion internals. It has to earn a push-back by beating `eidos-agi/aidd` on the frozen suite: public contracts, hard gates, held-out isolation, eval teeth, and the five legs remaining load-bearing.

Field ideas that v1 must not drop:

- Error analysis first, infrastructure second
- Three corpora: public / held-out / production-shadow
- Eval teeth (planted mutation must fail)
- pass^k, not one lucky green
- Future-weighted fitness (do not poison v2)
- Verifiability sets the automation ceiling

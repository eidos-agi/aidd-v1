# aidd-v1

Generation 1 of [eidos-agi/aidd](https://github.com/eidos-agi/aidd).

AI bashes here. The champion stays `aidd`. Winners get pushed back.

```
aidd        ← champion. public. evals live there.
aidd-v1     ← this repo. first bounded bash.
aidd-v2     ← not opened. wait for a trigger.
```

## Thesis

AIDD as versioned generation repos. Field evals as teeth. Do not clone champion internals. Satisfy the public contracts in `eidos-agi/aidd`.

## Rules

1. Evals are owned by [`eidos-agi/aidd`](https://github.com/eidos-agi/aidd). Read [EVALS.md](EVALS.md).
2. Do not edit held-out fixtures, thresholds, or critic calibration.
3. Failures become fixtures in the champion before `aidd-v2` opens.
4. Promotion is `/aidd-promote`. Human signs. AI does not promote.
5. This repo can lose. A higher number is not a successor.

See [GENERATION.md](GENERATION.md).
